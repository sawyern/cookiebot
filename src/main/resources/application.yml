spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CookieBot
    username: postgres
    password: 2DoRblt2
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
security:
  ignored: /**
